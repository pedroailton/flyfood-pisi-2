from itertools import permutations

# A biblioteca é sugestão do Gemini

def _calcular_distancia(ponto_a, ponto_b):
    # Lógica da distância de Manhattan: |x2 - x1| + |y2 - y1|
    pass

def _calcular_custo_total_da_rota(rota, pontos):
    # Lógica para somar as distâncias:
    # dist(R -> rota[0]) + dist(rota[0] -> rota[1]) + ... + dist(rota[-1] -> R)
    pass

def otimizar_rota(pontos):
    # 1. Separar 'R' dos outros pontos de entrega.
    # 2. Gerar todas as permutações dos pontos de entrega.
    # 3. Para cada permutação:
    #    - Calcular o custo total usando _calcular_custo_total_da_rota
    #    - Comparar com o menor custo encontrado até agora.
    # 4. Formatar a melhor rota encontrada em uma string.
    # 5. Retornar a string.
    pass